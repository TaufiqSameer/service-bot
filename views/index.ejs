<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Service Robot Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand d-flex align-items-center" href="#">
      <img src="/images/creative-blue-robot-orange-circle-logo-vector-22487773.jpg" alt="Logo" width="40" height="40" class="me-2 rounded-circle border border-light">
      Service Robot
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
        <li class="nav-item"><a class="nav-link" href="/control">Control</a></li>
        <li class="nav-item"><a class="nav-link" href="/logs">Logs</a></li>
        <li class="nav-item"><a class="nav-link" href="/about">About</a></li>
      </ul>
    </div>
  </div>
</nav>

<section class="bg-dark text-white text-center py-5">
  <div class="container">
    <h1 class="display-4">Meet the Service Robot 🤖</h1>
    <p class="lead mt-3">A smart assistant designed to deliver papers, snacks, and avoid obstacles efficiently inside our college!</p>
  </div>
</section>

<section class="container my-5">
  <h2 class="text-center mb-4">About the Project</h2>
  <div class="row">
    <div class="col-md-6">
      <p>
        This robot is built using a Raspberry Pi and sensors to help it navigate around the environment. 
        It collects data such as battery status, obstacle detection, and logs them to a database for monitoring.
      </p>
      <p>
        Users can control the robot via the web interface and monitor logs for performance and status updates.
      </p>
    </div>
    <div class="col-md-6">
      <div class="card shadow-sm">
        <div class="card-body">
          <h5 class="card-title">Project Team</h5>
          <ul>
            <li><strong>Developer:</strong> Mohammad Taufeeq Sameer (Software & Web Interface)</li>
            <li><strong>Team Members:</strong> Bhavana (Sensors), Sreeja (Mechanics), Harsha (3D design)</li>
            <li><strong>College Project (2025)</strong></li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</section>

<div class="text-center my-4">
  <form action="/move" method="POST">
    <button type="submit" class="btn btn-success btn-lg">Move Robot</button>
  </form>
</div>

<div class="container mt-5">
  <h2 class="text-center mb-4">Robot Status Logs</h2>

  <% if (data.length > 0) { %>
    <% data.forEach(item => { %>
      <div class="card mb-3 shadow-sm">
        <div class="card-body">
          <p class="card-text"><strong>Battery:</strong> <%= item.battery %>%</p>
          <p class="card-text"><strong>Obstacle:</strong> <%= item.obstacle ? "Yes" : "No" %></p>
          <p class="card-text"><strong>Timestamp:</strong> <%= new Date(item.timestamp).toLocaleString() %></p>
        </div>
      </div>
    <% }) %>
  <% } else { %>
    <div class="alert alert-warning text-center" role="alert">
      No robot status logs available.
    </div>
  <% } %>

  <hr class="my-4">

  <div class="container my-4">
    <div class="card shadow-sm">
      <div class="card-header bg-primary text-white">
        Assign a New Task to Robot
      </div>
      <div class="card-body">
        <form action="/order" method="POST">
          <div class="mb-3">
            <label for="task" class="form-label">Task</label>
            <input type="text" class="form-control" id="task" name="task" placeholder="e.g., Take papers" required>
          </div>
          <div class="mb-3">
            <label for="destination" class="form-label">Destination</label>
            <input type="text" class="form-control" id="destination" name="destination" placeholder="e.g., Staff Room" required>
          </div>
          <div class="d-flex justify-content-center">
            <button type="submit" class="btn btn-success">Submit Task</button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <div class="container my-4">
    <h4 class="mb-4 text-center fw-bold text-primary">📋 Recent Robot Orders</h4>

    <% if (orders.length > 0) { %>
      <div class="row row-cols-1 row-cols-md-2 g-3">
        <% orders.forEach(order => { %>
          <div class="col">
            <div class="card border-0 shadow-sm h-100">
              <div class="card-body">
                <h5 class="card-title text-success">
                  <i class="bi bi-check2-circle"></i> <%= order.task %>
                </h5>
                <p class="card-text">
                  <strong>Destination:</strong> <%= order.destination %>
                </p>
                <p class="card-text">
                  <small class="text-muted">🕒 Ordered at: <%= new Date(order.ordered_at).toLocaleString() %></small>
                </p>
              </div>
            </div>
          </div>
        <% }) %>
      </div>
    <% } else { %>
      <div class="alert alert-info text-center" role="alert">
        No recent orders found.
      </div>
    <% } %>
  </div>

  <div class="container my-5">
    <h2 class="text-center mb-4">📍 Robot Current Location</h2>
    <div id="map" style="height: 400px; border-radius: 8px; overflow: hidden;"></div>
  </div>

</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyBIelp2fQ-Bh5zzBNLKnLMDzED7mSthX6w"></script>
<script>
  function initMap() {
    var robotLocation = { lat: 12.9716, lng: 77.5946 };

    var map = new google.maps.Map(document.getElementById('map'), {
      zoom: 17,
      center: robotLocation
    });

    var marker = new google.maps.Marker({
      position: robotLocation,
      map: map,
      title: 'Service Robot 🤖'
    });
  }

  window.onload = initMap;
</script>

</body>
</html>
